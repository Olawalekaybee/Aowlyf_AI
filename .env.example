(these are placeholder values only, safe to commit — nobody's real password is in here):
POSTGRES_PASSWORD=changeme 
MINIO_ROOT_USER=impactlab-admin 
MINIO_ROOT_PASSWORD=changeme 
DATABASE_URL=postgresql://impactlab:changeme@localhost:5432/impactlab 
# WhatsApp reminders (Phase 4 — Twilio WhatsApp Business API) 
TWILIO_ACCOUNT_SID= 
TWILIO_AUTH_TOKEN= 
TWILIO_WHATSAPP_FROM=whatsapp:+1415XXXXXXX 
# Admin bootstrap — the one account with full visibility/control 
ADMIN_EMAIL=you@impactlab.org